CREATE TABLE Vehicle (
                         id INT PRIMARY KEY,
                         name VARCHAR(255),
                         vehicle_brand VARCHAR(255),
                         vehicle_type VARCHAR(255),
                         year VARCHAR(4)
);